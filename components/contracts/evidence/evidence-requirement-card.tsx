import type {ContractEvidenceReadModel,ContractEvidenceRequirement} from "@/modules/contracts/contract-evidence-read-model";
import {EvidenceCurrentDocument} from "./evidence-current-document";
import {EvidenceHistory} from "./evidence-history";
import {EvidenceStatusBadge} from "./evidence-status-badge";

type Props={requirement:ContractEvidenceRequirement;downloadingId:string|null;mutatingId:string|null;canWrite:boolean;onDownload:(evidence:ContractEvidenceReadModel)=>void;onUpload:(requirement:ContractEvidenceRequirement)=>void;onValidate:(evidence:ContractEvidenceReadModel)=>void;onInvalidate:(evidence:ContractEvidenceReadModel)=>void;onSupersede:(evidence:ContractEvidenceReadModel)=>void};
export function EvidenceRequirementCard({requirement,downloadingId,mutatingId,canWrite,onDownload,onUpload,onValidate,onInvalidate,onSupersede}:Props){const current=requirement.lineage.current;return <article className="min-w-0 rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div className="min-w-0"><h3 className="font-semibold text-slate-950">{requirement.label}</h3><p className="mt-1 text-sm leading-6 text-slate-500">{requirement.description}</p></div><EvidenceStatusBadge state={requirement.state}/></div>{requirement.reserved?<p className="mt-4 rounded-lg border border-sky-200 bg-sky-50 p-3 text-sm text-sky-900">Este requisito é somente informativo neste checkpoint e não representa uma pendência documental operacional. Reservado ao fluxo financeiro.</p>:current?<EvidenceCurrentDocument evidence={current} isDownloading={downloadingId===current.evidenceId} isMutating={mutatingId===current.evidenceId} canWrite={canWrite} onDownload={onDownload} onValidate={onValidate} onInvalidate={onInvalidate} onSupersede={onSupersede}/>:<div className="mt-4 rounded-lg border border-dashed border-slate-300 p-4"><p className="text-sm text-slate-500">Ainda não existe evidência corrente para este requisito.</p>{canWrite?<button className="mt-3 rounded-md bg-slate-900 px-3 py-2 text-sm font-semibold text-white" onClick={()=>onUpload(requirement)} type="button">Adicionar documento</button>:null}</div>}<EvidenceHistory history={requirement.lineage.history} downloadingId={downloadingId} onDownload={onDownload}/></article>}
